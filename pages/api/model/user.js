{
    user: {
      firstName: string
      lastName: string
      email: string
      password: string
    },
    expires: Date // This is the expiry of the session, not any of the tokens within the session
  }